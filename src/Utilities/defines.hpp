
namespace xppr {
    using byte_t = char;

#ifndef NDEBUG
#define IFDEBUG(...) __VA_ARGS__
#else
#define IFDEBUG(...)
#endif

// timestamp: [ERROR] module: 

log ("F returned error {}", error);

enum class EngineError
{}; 
}